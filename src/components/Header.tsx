import React from "react";

interface Props {

}

const Header = (props: Props) => {
    return (
        <header>
            <h1>React + Todo TS</h1>
        </header>
    )
}

export default Header